package fi.ni;

import java.net.URI;

import org.junit.Test;

import fi.aalto.drumbeat.security.concepts.Organization;
import fi.ni.concepts.DataStore;
import fi.ni.webid.WebIDCertificate;
import junit.framework.TestCase;

public class AuthorizationTests extends TestCase {
/*
	@Test
	public void test_basicConnection() {
		Exception ex = null;
		try {
			Organization o = (Organization) Internet.get("http://company1/");
			WebIDCertificate wc = o.getWebID("mats");
			DataStore ds1 = new DataStore("http://datastore/");
			if (ds1.connect(wc, "collection1/datastore1/object1") == true)
				System.out.println("Connected...");
			else
				fail("Should be connected!");

		} catch (Exception e) {
			ex = e;
		}
		assertEquals(null, ex);
	}
	
	@Test
	public void test_unknownUser() {
		Exception ex = null;
		try {
			URI uri=new URI("http://unknown/user1");
			WebIDCertificate wc = new WebIDCertificate(uri, "jshjshjshjsh");
			DataStore ds1 = new DataStore("http://datastore/");
			if (ds1.connect(wc, "collection1/datastore1/object1") == true)
				fail("Should not be connected!");
		} catch (Exception e) {
			ex = e;
		}
		assertEquals(null, ex);
	}
*/
}
